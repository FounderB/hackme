[Service]
# Page cache for coordinator.db (~1.3G) + coordinator_fuzz.db (~1.9G) is charged to this cgroup.
# Old MemoryMax=2G OOM-killed a healthy ~200MB Go heap under submit load (10 kills / 2d).
MemoryHigh=2.2G
MemoryMax=3.0G
